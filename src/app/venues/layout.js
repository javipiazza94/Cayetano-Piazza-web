export const metadata = {
    title: 'Espacios | Glory Nights',
    description: 'Descubre las salas y espacios únicos donde Glory Nights celebra sus conciertos tributo a la luz de las velas.',
};

export default function VenuesLayout({ children }) {
    return children;
}
